import type { components, paths } from "./jellyfin.generated"; // generated by openapi-typescript
import createClient from "openapi-fetch";
import { query } from "@solidjs/router";
import {
  Entry,
  PlayerAdjacentItem,
  PlayerContextualAction,
  PlayerMetadata,
  PlayerPreviewTileMetadata,
  PlayerTimelineMarker,
  PlayerTrackOption,
} from "../types";
import { CamelCased, mapKeysToCamelCase } from "~/utilities";

type ItemImageType =
  | "Primary"
  | "Art"
  | "Backdrop"
  | "Banner"
  | "Logo"
  | "Thumb"
  | "Disc"
  | "Box"
  | "Screenshot"
  | "Menu"
  | "Chapter"
  | "BoxRear"
  | "Profile";

type OidcProviderConfig = Pick<
  components["schemas"]["OidConfig"],
  "CanonicalLinks"
>;

const getBaseUrl = () => {
  "use server";

  return process.env.JELLYFIN_BASE_URL;
};

const isConfigured = () => {
  "use server";

  return Boolean(getBaseUrl() && process.env.JELLYFIN_API_KEY);
};

const getClient = () => {
  "use server";

  return createClient<paths>({
    baseUrl: getBaseUrl(),
    headers: {
      Authorization: `MediaBrowser DeviceId="Streamarr", Token="${process.env.JELLYFIN_API_KEY}"`,
      "Content-Type": 'application/json; profile="CamelCase"',
    },
  });
};

const getJson = async <T>(path: string): Promise<T | undefined> => {
  "use server";

  const baseUrl = getBaseUrl();

  if (!baseUrl || !process.env.JELLYFIN_API_KEY) {
    return undefined;
  }

  const response = await fetch(new URL(path, baseUrl), {
    headers: {
      Authorization: `MediaBrowser DeviceId="Streamarr", Token="${process.env.JELLYFIN_API_KEY}"`,
      "Content-Type": 'application/json; profile="CamelCase"',
    },
  });

  if (!response.ok) {
    throw new Error(`Failed to fetch Jellyfin API ${path}: ${response.status}`);
  }

  return response.json() as Promise<T>;
};

const getProxiedResponse = async (path: string): Promise<Response> => {
  "use server";

  const baseUrl = getBaseUrl();

  if (!baseUrl || !process.env.JELLYFIN_API_KEY) {
    return new Response("Jellyfin is not configured", { status: 503 });
  }

  const response = await fetch(new URL(path, baseUrl), {
    headers: {
      Authorization: `MediaBrowser DeviceId="Streamarr", Token="${process.env.JELLYFIN_API_KEY}"`,
    },
  });

  if (!response.ok) {
    return new Response(response.body, {
      status: response.status,
      headers: response.headers,
    });
  }

  return new Response(response.body, {
    status: response.status,
    headers: response.headers,
  });
};

export const getLinkedUserId = async (
  canonicalName: string,
): Promise<string | undefined> => {
  "use server";

  const providers = await getJson<Record<string, OidcProviderConfig>>("/SSO/OID/Get");

  return providers?.zitadel?.CanonicalLinks?.[canonicalName];
};

export const getCurrentUser = query(async () => {
  "use server";

  if (!isConfigured()) {
    return [];
  }

  const { data } = await getClient().GET("/Users/Public", {
    params: {},
  });

  return data;
}, "jellyfin.getCurrentUser");

export const listUsers = query(async () => {
  "use server";

  if (!isConfigured()) {
    return [];
  }

  const { data, error } = await getClient().GET("/Users", {
    params: {},
  });

  return data ?? [];
}, "jellyfin.listUsers");

export const listItemIds = query(
  async (): Promise<Record<string, { jellyfin: string }>> => {
    "use server";

    if (!isConfigured()) {
      return {};
    }

    const { data, error } = await getClient().GET("/Items", {
      params: {
        query: {
          hasImdbId: true,
          recursive: true,
          includeItemTypes: ["Movie", "Series"],
          fields: [
            "ProviderIds",
            "Genres",
            "DateLastMediaAdded",
            "DateCreated",
            "MediaSources",
          ],
        },
      },
    });

    if (data === undefined) {
      return {};
    }

    return Object.fromEntries(
      data.Items?.map((item) => {
        const type = {
          Movie: 'm',
          Series: 's',
        }[item.Type as string] ?? '';

        return [
          `${type}${item.ProviderIds!["Tmdb"]!}`,
          { jellyfin: item.Id! },
        ];
      }) ?? []
    );
  },
  "jellyfin.listItemIds",
);

export const listItems = query(
  async (userId: string): Promise<Entry[] | undefined> => {
    "use server";

    if (!isConfigured()) {
      return undefined;
    }

    const { data, error } = await getClient().GET("/Items", {
      params: {
        query: {
          userId,
          hasTmdbInfo: true,
          recursive: true,
          includeItemTypes: ["Movie", "Series"],
          fields: [
            "ProviderIds",
            "Genres",
            "DateLastMediaAdded",
            "DateCreated",
            "MediaSources",
          ],
        },
      },
    });

    if (data === undefined) {
      return undefined;
    }

    return (
      data.Items?.map((item) => toEntry(item)) ?? []
    );
  },
  "jellyfin.listItems",
);

export const getRandomItem = query(
  async (userId: string): Promise<Entry | undefined> => {
    "use server";

    return getRandomItems(userId, 1).then((items) => items?.at(0));
  },
  "jellyfin.listRandomItem",
);

export const getRandomItems = query(
  async (userId: string, limit: number = 20): Promise<Entry[]> => {
    "use server";

    if (!isConfigured()) {
      return [];
    }

    const { data, error } = await getClient().GET("/Items", {
      params: {
        query: {
          userId,
          hasTmdbInfo: true,
          recursive: true,
          limit,
          sortBy: ["Random"],
          includeItemTypes: ["Movie", "Series"],
          imageTypes: ["Primary", "Backdrop", "Thumb"],
          fields: [
            "ProviderIds",
            "Genres",
            "DateLastMediaAdded",
            "DateCreated",
            "MediaSources",
          ],
        },
      },
    });

    return (
      data?.Items?.map((item) => toEntry(item)) ?? []
    );
  },
  "jellyfin.listRandomItems",
);

export const getItem = query(
  async (userId: string, itemId: string): Promise<Entry | undefined> => {
    "use server";

    if (!isConfigured()) {
      return undefined;
    }

    const { data, error } = await getClient().GET("/Items/{itemId}", {
      params: {
        path: {
          itemId,
        },
        query: {
          userId,
          hasTmdbInfo: true,
          recursive: true,
          includeItemTypes: ["Movie", "Series"],
          fields: [
            "ProviderIds",
            "Genres",
            "DateLastMediaAdded",
            "DateCreated",
            "MediaSources",
          ],
        },
      },
    });

    if (data === undefined) {
      return undefined;
    }

    return toEntry(data);
  },
  "jellyfin.getItem",
);

export const getItemStream = query(
  async (userId: string, itemId: string, range: string): Promise<Response> => {
    "use server";

    if (!isConfigured()) {
      return new Response("Jellyfin is not configured", { status: 503 });
    }

    const item = await resolvePlayableItem(userId, itemId);

    if (!item?.Id) {
      return new Response("Playable item not found", { status: 404 });
    }

    const { response } = await getClient().GET("/Videos/{itemId}/stream", {
      params: {
        path: {
          itemId: item.Id,
        },
        query: {
        },
      },
      parseAs: 'stream',
      headers: {
        Range: range
      }
    });

    return response;
  },
  "jellyfin.getItemStream",
);

export const getPlayerMetadata = query(
  async (
    userId: string,
    entryId: string,
    itemId: string,
  ): Promise<PlayerMetadata | undefined> => {
    "use server";

    if (!isConfigured()) {
      return;
    }

    const item = await resolvePlayableItem(userId, itemId);

    if (!item?.Id) {
      return;
    }

    const [playbackInfo, userData, segments, adjacent] = await Promise.all([
      getPlaybackInfo(userId, item.Id),
      getItemUserData(userId, item.Id),
      getItemSegments(item.Id),
      getAdjacentItems(userId, item),
    ] as const);
    const mediaSource =
      playbackInfo?.MediaSources?.find((source) => source.Id) ??
      item.MediaSources?.find((source) => source.Id) ??
      item.MediaSources?.at(0) ??
      playbackInfo?.MediaSources?.at(0);

    if (!mediaSource) {
      return;
    }

    const streams = mediaSource.MediaStreams ?? item.MediaStreams ?? [];
    const videoStream = streams.find((stream) => stream.Type === "Video");
    const audioStreams = streams.filter((stream) => stream.Type === "Audio");
    const subtitleStreams = streams.filter((stream) => stream.Type === "Subtitle");
    const defaultAudioIndex =
      mediaSource.DefaultAudioStreamIndex ??
      audioStreams.find((stream) => stream.IsDefault)?.Index ??
      audioStreams.at(0)?.Index;
    const defaultSubtitleIndex = mediaSource.DefaultSubtitleStreamIndex;
    const markers = [
      ...toChapterMarkers(item.Chapters),
      ...toSegmentMarkers(segments),
    ];

    return {
      id: toPlayerItemId(item.Id),
      entryId,
      jellyfinItemId: item.Id,
      mediaSourceId: mediaSource.Id ?? undefined,
      title: item.Name ?? "Untitled",
      poster: imageUrl(item.Id, "Backdrop"),
      streamUrl: `/api/content/${toPlayerItemId(item.Id)}/stream`,
      resumeOffset: ticksToSeconds(userData?.playbackPositionTicks ?? 0),
      duration: ticksToSeconds(mediaSource.RunTimeTicks ?? item.RunTimeTicks ?? 0),
      directPlay: {
        supported: mediaSource.SupportsDirectPlay !== false,
        container: mediaSource.Container ?? item.Container ?? undefined,
        videoCodec: videoStream?.Codec ?? undefined,
        audioCodec:
          audioStreams.find((stream) => stream.Index === defaultAudioIndex)?.Codec ??
          audioStreams.at(0)?.Codec ??
          undefined,
        audioChannels:
          audioStreams.find((stream) => stream.Index === defaultAudioIndex)?.Channels ??
          audioStreams.at(0)?.Channels ??
          undefined,
        width: videoStream?.Width ?? undefined,
        height: videoStream?.Height ?? undefined,
        reasons: mediaSource.SupportsDirectPlay === false
          ? ["Jellyfin did not report direct-play support for this media source."]
          : [],
      },
      audioTracks: audioStreams.map((stream) =>
        toAudioTrack(stream, defaultAudioIndex),
      ),
      subtitleTracks: [
        {
          id: "off",
          index: -1,
          label: "Off",
          default: defaultSubtitleIndex == null,
          availability: defaultSubtitleIndex == null ? "selected" : "available",
        },
        ...subtitleStreams.map((stream) =>
          toSubtitleTrack(item.Id!, mediaSource.Id ?? item.Id!, stream, defaultSubtitleIndex),
        ),
      ],
      preview: toPreviewTileMetadata(item.Id, item.Trickplay),
      previous: adjacent.previous,
      next: adjacent.next,
      markers,
      contextualActions: toContextualActions(segments, adjacent.next),
    };
  },
  "jellyfin.getPlayerMetadata",
);

export const getSubtitleAsset = query(
  async (
    itemId: string,
    mediaSourceId: string,
    index: number,
  ): Promise<Response> => {
    "use server";

    return getProxiedResponse(
      `/Videos/${itemId}/${mediaSourceId}/Subtitles/${index}/0/Stream.vtt`,
    );
  },
  "jellyfin.getSubtitleAsset",
);

export const getTrickplayTile = query(
  async (itemId: string, width: number, index: number): Promise<Response> => {
    "use server";

    return getProxiedResponse(`/Videos/${itemId}/Trickplay/${width}/${index}.jpg`);
  },
  "jellyfin.getTrickplayTile",
);

interface ItemUserData extends CamelCased<components["schemas"]["UserItemDataDto"]> {}

export const getItemUserData = query(
  async (userId: string, itemId: string): Promise<ItemUserData|undefined> => {
    "use server";

    if (!isConfigured() || !itemId) {
      return;
    }

    const { data } = await getClient().GET('/UserItems/{itemId}/UserData', {
      params: {
        path: {
          itemId,
        },
        query: {
          userId,
        }
      },
    });

    if (!data) {
      return;
    }

    return mapKeysToCamelCase(data);
  },
  "jellyfin.getItemUserData",
);

export const getItemImage = query(
  async (
    itemId: string,
    imageType: ItemImageType,
  ): Promise<any | undefined> => {
    "use server";

    if (!isConfigured()) {
      return;
    }

    const { data, error } = await getClient().GET(
      "/Items/{itemId}/Images/{imageType}",
      {
        parseAs: "blob",
        params: {
          path: {
            itemId,
            imageType,
          },
          query: {},
        },
      },
    );

    return data;
  },
  "jellyfin.getItemImage",
);

export const getItemPlaybackInfo = query(
  async (userId: string, itemId: string): Promise<components["schemas"]["PlaybackInfoResponse"] | undefined> => {
    "use server";

    if (!isConfigured()) {
      return;
    }

    return getPlaybackInfo(userId, itemId);
  },
  "jellyfin.getItemPlaybackInfo",
);

export const queryItems = query(async () => {
  "use server";

  if (!isConfigured()) {
    return;
  }

  const { data, error } = await getClient().GET("/Items", {
    params: {
      query: {
        mediaTypes: ["Video"],
        isUnaired: true,
        limit: 10,
        // fields: ["ProviderIds", "Genres"],
        includeItemTypes: ["Series", "Movie"],
        recursive: true,
      },
    },
  });
}, "jellyfin.queryItems");

export const getContinueWatching = query(
  async (userId: string): Promise<Entry[]> => {
    "use server";

    if (!isConfigured()) {
      return [];
    }

    const { data, error } = await getClient().GET("/UserItems/Resume", {
      params: {
        query: {
          userId,
          mediaTypes: ["Video"],
          // fields: ["ProviderIds", "Genres"],
          // includeItemTypes: ["Series", "Movie"]
        },
      },
    });

    if (Array.isArray(data?.Items) !== true) {
      return [];
    }

    const uniqueIds = new Set<string>(
      data.Items.map((item) =>
        item.Type === "Episode" ? item.SeriesId! : item.Id!,
      ),
    );
    const results = await Promise.allSettled(
      uniqueIds
        .values()
        .map((id) => getItem(userId, id))
        .toArray(),
    );

    assertNoErrors(results);

    return results
      .filter(
        (result): result is PromiseFulfilledResult<Entry> =>
          result.value !== undefined,
      )
      .map(({ value }) => value);
  },
  "jellyfin.continueWatching",
);

function assertNoErrors<T>(
  results: PromiseSettledResult<T>[],
): asserts results is PromiseFulfilledResult<T>[] {
  if (results.some(({ status }) => status !== "fulfilled")) {
    throw new Error("one or more promices failed", {
      cause: results
        .filter((r): r is PromiseRejectedResult => r.status === "rejected")
        .map((r) => r.reason),
    });
  }
}

const toEntry = (item: components['schemas']['BaseItemDto']): Entry => {
  const type = {
    Movie: 'm',
    Series: 's',
  }[item.Type as string] as any;

  return {
    id: `${type}${item.ProviderIds!["Tmdb"]!}`,
    title: item.Name!,
    overview: item.Overview!,
    thumbnail: new URL(`/Items/${item.Id}/Images/Primary`, getBaseUrl()),
    image: new URL(`/Items/${item.Id}/Images/Backdrop`, getBaseUrl()),
    trailer: item.RemoteTrailers?.at(-1)?.Url ?? undefined,
  };
};

  type JellyfinItem = components["schemas"]["BaseItemDto"];
  type MediaStream = components["schemas"]["MediaStream"];
  type MediaSegment = components["schemas"]["MediaSegmentDto"];
  type TrickplayMap = NonNullable<JellyfinItem["Trickplay"]>;

  const PLAYBACK_ITEM_PREFIX = "jf:";

  export const toPlayerItemId = (itemId: string) => `${PLAYBACK_ITEM_PREFIX}${itemId}`;

  export const fromPlayerItemId = (id: string) =>
    id.startsWith(PLAYBACK_ITEM_PREFIX) ? id.slice(PLAYBACK_ITEM_PREFIX.length) : undefined;

  const getPlaybackInfo = async (userId: string, itemId: string) => {
    "use server";

    const { data } = await getClient().POST("/Items/{itemId}/PlaybackInfo", {
      params: {
        path: { itemId },
        query: { userId },
      },
      body: {
        UserId: userId,
        EnableDirectPlay: true,
        EnableDirectStream: false,
        EnableTranscoding: false,
        AllowAudioStreamCopy: false,
        AllowVideoStreamCopy: false,
        AutoOpenLiveStream: false,
      },
    });

    return data;
  };

  const getItemWithPlaybackFields = async (userId: string, itemId: string) => {
    "use server";

    const { data } = await getClient().GET("/Items/{itemId}", {
      params: {
        path: { itemId },
        query: {
          userId,
          fields: ["Chapters", "Trickplay", "MediaSources", "MediaStreams"],
        },
      },
    });

    return data;
  };

  const resolvePlayableItem = async (userId: string, itemId: string) => {
    "use server";

    const item = await getItemWithPlaybackFields(userId, itemId);

    if (item?.Type !== "Series") {
      return item;
    }

    const { data } = await getClient().GET("/Shows/{seriesId}/Episodes", {
      params: {
        path: { seriesId: item.Id! },
        query: {
          userId,
          fields: ["Chapters", "Trickplay", "MediaSources", "MediaStreams"],
          enableUserData: true,
        },
      },
    });
    const episodes = [...(data?.Items ?? [])].sort(compareEpisodes);

    return (
      episodes.find((episode) => (episode.UserData?.PlaybackPositionTicks ?? 0) > 0) ??
      episodes.find((episode) => episode.UserData?.Played !== true) ??
      episodes.at(0)
    );
  };

  const getItemSegments = async (itemId: string): Promise<MediaSegment[]> => {
    "use server";

    const { data, response } = await getClient().GET("/MediaSegments/{itemId}", {
      params: {
        path: { itemId },
      },
    });

    if (response.status === 404) {
      return [];
    }

    return data?.Items ?? [];
  };

  const getAdjacentItems = async (
    userId: string,
    item: JellyfinItem,
  ): Promise<{ previous?: PlayerAdjacentItem; next?: PlayerAdjacentItem }> => {
    "use server";

    if (item.Type !== "Episode" || !item.SeriesId || !item.Id) {
      return {};
    }

    const { data } = await getClient().GET("/Shows/{seriesId}/Episodes", {
      params: {
        path: { seriesId: item.SeriesId },
        query: {
          userId,
          adjacentTo: item.Id,
          fields: ["MediaSources"],
          enableUserData: true,
        },
      },
    });
    const episodes = [...(data?.Items ?? [])].sort(compareEpisodes);
    const currentIndex = episodes.findIndex((episode) => episode.Id === item.Id);

    return {
      previous: toAdjacentItem(episodes.at(currentIndex - 1)),
      next: toAdjacentItem(episodes.at(currentIndex + 1)),
    };
  };

  const toAdjacentItem = (item?: JellyfinItem): PlayerAdjacentItem | undefined => {
    if (!item?.Id) {
      return;
    }

    return {
      id: toPlayerItemId(item.Id),
      title: item.Name ?? "Untitled",
      streamUrl: `/api/content/${toPlayerItemId(item.Id)}/stream`,
      resumeOffset: ticksToSeconds(item.UserData?.PlaybackPositionTicks ?? 0),
    };
  };

  const toAudioTrack = (
    stream: MediaStream,
    selectedIndex: number | null | undefined,
  ): PlayerTrackOption => ({
    id: String(stream.Index),
    index: stream.Index ?? -1,
    label: trackLabel(stream),
    language: stream.Language ?? undefined,
    codec: stream.Codec ?? undefined,
    default: stream.IsDefault === true,
    availability: stream.Index === selectedIndex ? "selected" : "unavailable",
    reason:
      stream.Index === selectedIndex
        ? undefined
        : "Changing embedded audio tracks requires a future playback mode.",
  });

  const toSubtitleTrack = (
    itemId: string,
    mediaSourceId: string,
    stream: MediaStream,
    selectedIndex: number | null | undefined,
  ): PlayerTrackOption => {
    const isText = stream.IsTextSubtitleStream === true || textSubtitleCodecs.has((stream.Codec ?? "").toLowerCase());
    const index = stream.Index ?? -1;

    return {
      id: String(index),
      index,
      label: trackLabel(stream),
      language: stream.Language ?? undefined,
      codec: stream.Codec ?? undefined,
      default: stream.IsDefault === true,
      forced: stream.IsForced === true,
      availability: !isText
        ? "unavailable"
        : index === selectedIndex
          ? "selected"
          : "available",
      reason: isText ? undefined : "Only browser-compatible text subtitles are available in this player.",
      url: isText ? `/api/content/${toPlayerItemId(itemId)}/subtitles/${mediaSourceId}/${index}.vtt` : undefined,
    };
  };

  const toPreviewTileMetadata = (
    itemId: string,
    trickplay?: TrickplayMap | null,
  ): PlayerPreviewTileMetadata | undefined => {
    const [width, info] = Object.entries(trickplay ?? {})
      .map(([width, info]) => [Number(width), info] as const)
      .filter(([, info]) => info.Width && info.Height && info.TileWidth && info.TileHeight && info.Interval)
      .sort(([a], [b]) => a - b)
      .at(0) ?? [];

    if (!width || !info?.Width || !info.Height || !info.TileWidth || !info.TileHeight || !info.Interval) {
      return;
    }

    return {
      width: info.Width,
      height: info.Height,
      tileWidth: info.TileWidth,
      tileHeight: info.TileHeight,
      thumbnailCount: info.ThumbnailCount ?? 0,
      interval: info.Interval / 1000,
      tileUrl: `/api/content/${toPlayerItemId(itemId)}/trickplay/${width}/{index}.jpg`,
    };
  };

  const toChapterMarkers = (chapters?: JellyfinItem["Chapters"] | null): PlayerTimelineMarker[] =>
    chapters?.map((chapter, index) => ({
      id: `chapter-${index}`,
      type: "chapter",
      label: chapter.Name ?? `Chapter ${index + 1}`,
      start: ticksToSeconds(chapter.StartPositionTicks ?? 0),
    })) ?? [];

  const toSegmentMarkers = (segments: MediaSegment[]): PlayerTimelineMarker[] =>
    segments
      .filter((segment) => segment.Type && segment.Type !== "Unknown")
      .map((segment) => ({
        id: segment.Id ?? `segment-${segment.StartTicks}`,
        type: "segment",
        label: segment.Type!,
        start: ticksToSeconds(segment.StartTicks ?? 0),
        end: ticksToSeconds(segment.EndTicks ?? 0),
      }));

  const toContextualActions = (
    segments: MediaSegment[],
    next?: PlayerAdjacentItem,
  ): PlayerContextualAction[] => [
    ...segments.flatMap((segment) => {
      const start = ticksToSeconds(segment.StartTicks ?? 0);
      const end = ticksToSeconds(segment.EndTicks ?? 0);

      if (!segment.Type || start >= end) {
        return [];
      }

      const segmentActions: Partial<Record<NonNullable<MediaSegment["Type"]>, PlayerContextualAction["type"]>> = {
        Intro: "skip-intro",
        Recap: "skip-recap",
        Outro: "skip-outro",
      };
      const type = segmentActions[segment.Type];

      if (!type) {
        return [];
      }

      return [{
        id: segment.Id ?? `${type}-${start}`,
        type,
        label: type === "skip-intro" ? "Skip intro" : type === "skip-recap" ? "Skip recap" : "Skip outro",
        start,
        end,
        seekTo: end,
      }];
    }),
    ...(next
      ? [{
          id: `next-${next.id}`,
          type: "next-item" as const,
          label: "Next episode",
          start: Math.max(0, (segments.find((segment) => segment.Type === "Outro")?.StartTicks ?? 0) / 10_000_000),
          end: Number.POSITIVE_INFINITY,
          itemId: next.id,
        }]
      : []),
  ];

  const trackLabel = (stream: MediaStream) =>
    [
      stream.DisplayTitle,
      stream.Language?.toUpperCase(),
      stream.Codec?.toUpperCase(),
      stream.Channels ? `${stream.Channels}ch` : undefined,
      stream.IsForced ? "Forced" : undefined,
      stream.IsHearingImpaired ? "SDH" : undefined,
    ]
      .filter(Boolean)
      .join(" · ") || `Track ${stream.Index}`;

  const compareEpisodes = (a: JellyfinItem, b: JellyfinItem) =>
    (a.ParentIndexNumber ?? 0) - (b.ParentIndexNumber ?? 0) ||
    (a.IndexNumber ?? 0) - (b.IndexNumber ?? 0) ||
    (a.SortName ?? a.Name ?? "").localeCompare(b.SortName ?? b.Name ?? "");

  const imageUrl = (itemId: string, type: ItemImageType) =>
    getBaseUrl() ? String(new URL(`/Items/${itemId}/Images/${type}`, getBaseUrl())) : undefined;

  const textSubtitleCodecs = new Set(["srt", "subrip", "vtt", "webvtt", "ass", "ssa"]);

  const ticksToSeconds = (ticks: number) => ticks / 10_000_000;